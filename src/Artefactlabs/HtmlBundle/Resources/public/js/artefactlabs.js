define([
    "config",
    "jquery",
    "bootstrap"
], function(config, $, bootstrap) {

    return {
        init: function() {
            console.dir(config);
            console.log("artefactlabs init success");
        }
    };

});